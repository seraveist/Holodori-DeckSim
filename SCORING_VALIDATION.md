# 범용 유닛 계산 검증 — v1.2.0 / 2026-09-10

BJ까지의 사용자 실측을 바탕으로 선택한 계산식을 서비스 엔진에 반영했다. 인게임 상세 표시를 재현하는 경험 모형이며 게임 내부 공식의 확정은 아니다.

## 확인 범위

| 항목 | 실제 서비스 엔진 대조 |
| --- | --- |
| 패시브·홀로멤 보드 동시 일치 | 적용 대상 53건 중 52건 |
| 액티브 | 위 범위에서 값이 제공된 49건 모두 일치 |
| SP | 위 범위에서 값이 제공된 49건 모두 일치 |
| 서로 다른 멤버 5명 조합 | 27개 |
| AX / BC·BI 재확인 | 보드 계산 10.8%, 실측 10.9%; 패시브 없음 |

53건에는 두 행만 확인한 부분 측정과 반복 편성이 포함된다. BC·BI는 AX 재확인으로 별도 53건에 더하지 않는다. 후보 선택에 사용한 관측과 이후 사전 예측 성공이 섞여 있으므로 52/53을 무작위 표본의 정확도 보장으로 읽으면 안 된다. 스코어 서포트 의상 I/J/M 및 리더 보드 입력이 불확실한 U는 배분식 적용 대상에서 제외했다.

과거에 확인한 멤버 파라미터, 파라미터 의상, 패시브 종합력의 회귀는 유지한다. 이 검증에서 보드·메모리·강화 종합력, 실제 최종 유닛스코어 및 곡 플레이 점수를 새로 실측한 것은 아니다. 기존 환산 상수 K=2.037342와 그 회귀 자료는 그대로 사용한다.

## 서비스 수식

실행 구현은 [js/unit-score.js](js/unit-score.js), 카드 조건·패시브 대상·계정 입력 연결은 [js/score.js](js/score.js)다. 모형 ID는 `normalized-200-v1`이다.

1. 200초의 정수 시점 1~200에서 액티브 지속 구간을 평가한다. 동시에 유효한 멤버의 발동확률 합이 1보다 크면 그 합으로 각 확률을 나눠 정규화한다. 첫 발동은 한 주기 후이며 지속 구간의 끝은 제외한다.
2. 기본 발동확률과 보드 적용 후 발동확률은 각각 0.1%p 단위로 올린다. 보드 적용 확률은 올림 전 기본 확률에 발동률 증가를 곱한 뒤 상한 100%를 적용한다. 보드 발동 주기는 `기본 주기 / (1 + 빈도 증가/100)`이다.
3. 각 멤버의 효과량을 0.1 단위로 올린다. 보정 효과량은 `기본 효과량 × (1 + (리더 보드 서포트 + 해당 멤버의 패시브 서포트)/100)`이다. 기본 시간 가중합 A와 보정 시간 가중합 BP를 각각 0.1 단위로 올린 뒤, `Q=ceil0.1(BP)-ceil0.1(A)`를 구한다.
4. 멤버별 `w=원래 확률 × 효과량 × 지속시간 / 원래 주기`로 패시브 기여 P와 보드 기여 B를 가중한다. `P=Σw×패시브 서포트`, `B=Σw×100×[(1+발동률/100)(1+빈도/100)(1+리더 보드 서포트/100)-1]`이다. Q를 P:B로 나누고 두 표시 항목을 각각 0.1 단위로 올린다. 부동소수점 경계 오차만 작은 수치 허용값으로 처리하며 AX 전용 보정은 없다.
5. SP는 멤버별 `ceil0.01(SP 서포트×지속시간/120)`의 합과 조건이 충족된 SP 발동률 증가 합/20을 더한 뒤, 표시 액티브에 곱해 100으로 나누고 0.1 단위로 올린다. 800노트·라이프 1000 조건 기준을 사용한다.

200초는 관측 재현 설정이다. 실제 게임이 유일하게 이 더미 악곡을 쓴다는 증거는 아니다. SP의 120 상수도 실제 SP 지점 5개의 위치를 특정하지 않는다.

## 계정 입력과 배치

엔진의 선택 입력 `accountBonuses`에 `leaderBoardSupportPct`와 `memberBoards[characterId].activationRatePct / activationFrequencyPct`를 전달하면 보드 간접 영향과 배분을 함께 계산한다. 기본값은 모두 0이며 현재 화면에는 상세 보드 편집기가 없다. 보드 강화는 노드 기본값과 강화율로 계산한 합계를 입력한다. 알려진 85% 강화의 6→11.1 / 2→3.7, 55% 강화의 4→6.2 / 2→3.1은 소수 한 자리에서 정확히 표현된다. 이것만으로 AX 차이의 원인을 확정하지 않는다.

기존 `boardScoreBonusPct`는 계산된 보드에 더하는 별도 직접 기여다. 상세 보드 입력과 같은 전체 보드 표시값을 다시 넣으면 중복된다. 미입력 보드·메모리 종합력은 포함하지 않으며 기존 멤버 강화 입력은 유지한다.

범용 상세의 기본 점수와 배치 비교는 구분된다. 악곡 미선택에서는 기존 110초·800노트·균등 SP 5지점의 공통 채보로 120개 순열을 비교해 잠재 점수가 최대인 대표 순서 하나를 고른다. 악곡 선택 시에는 해당 채보 및 사용자가 고른 평균/잠재 목표로 대표 순서를 정한다. 같은 조합을 여러 순서로 중복 표시하지 않는다.

잠재 유닛 계산은 유효 액티브의 전부 성공을 가정한 확장 추정이다. 발동 불가 스킬은 유지하고 SP 발동률 이득을 중복 가산하지 않으며, 기대값보다 낮아지지 않도록 한다. 이 잠재값 자체를 인게임 실측으로 검증한 것은 아니다. 악곡의 시간축 계산 방식은 유지하지만 기준 유닛 값이 바뀌므로 악곡 예상 점수도 달라질 수 있다.

## 남은 범위

- AX의 0.1%p 보드 차이는 재확인까지 그대로 보존했다. 전체 사례를 맞추려고 개별 카드 상수를 추가하지 않는다.
- 스코어 서포트 의상은 새 액티브·SP를 사용하되 의상 및 패시브 배분은 기존 추정값을 유지한다. 세부 결과의 `unitBonusModel=legacy-score-support-costume`으로 식별한다. 상세 보드 배분은 이 경우 적용하지 않는다.
- 미확인 판정 노드, 추가 보드 효과와 새로운 스킬 종류, 정확한 내부 정밀도·반올림 규칙은 범위 밖이다. 별도 추가 근거 없이 한 설정의 성공을 모든 계정에 일반화하지 않는다.

## 재현

Node.js 24 이상에서:

```sh
node scripts/test-unit-display.mjs
node scripts/run-scoring-validation.mjs
node scripts/check-version.mjs
```

첫 명령은 실제 서비스 엔진으로 [BJ까지의 최소 실측 fixture](scripts/fixtures/unit-display-through-BJ.json)를 대조한다. 알려진 AX 차이도 명시적으로 검사하며 관측이 없는 값은 성공 건수에 넣지 않는다. 마지막 대화의 BA~BJ 입력·사전 예측·관측은 [별도 기록](analysis/unit-score/observations/BA-BJ-20260910.json)에 보존했다.

전체 명령은 서비스 회귀를 먼저 실행한 뒤, `.local/scoring-validation/historical-*`에 별도 사본을 만들어 해시가 고정된 과거 연구를 원래 v0.9 엔진으로 실행한다. 아카이브 해시를 검증하고 원래 예측·실패 이력을 변경하지 않는다. 출력의 과거 미관측/실패 상태는 해당 시점 기록이다. 무거운 탐색까지 재현하려면 `--research-grid`를 추가한다. `node verify-handoff.mjs`도 같은 전체 검사를 실행한다.

이전 1차 검증과 AZ 인수인계는 [phase1 기록](analysis/unit-score/archive/SCORING_VALIDATION-phase1.md), [AZ 기록](analysis/unit-score/archive/HANDOFF-AZ.md)에 남겼다.
